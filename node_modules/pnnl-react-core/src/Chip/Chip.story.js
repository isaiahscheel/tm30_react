/* eslint import/no-extraneous-dependencies: ["error", {"devDependencies": true}] */
/* eslint-disable no-alert */
import 'whatwg-fetch';
import React from 'react';
import { storiesOf } from '@storybook/react'; // eslint-disable-line

import * as C from '../index';

storiesOf('Chip', module)
  .add('with label', () => (
    <C.Chip label="Chip Name" />
  ))
  .add('with color', () => (
    <C.Chip label="Chip Name" color="topaz" />
  ))
  .add('with uppercase label', () => (
    <C.Chip label="FOREIGN NATIONAL" color="ruby" />
  ))
  .add('with Avatar', () => {
    const chipAvatar = (<C.Avatar src="https://api.pnl.gov/operations/v2/portraits/self?type=image" size="xs" />);
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={chipAvatar} />
    );
  })
  .add('with text Avatar', () => {
    const chipAvatar = (<C.Avatar size="xs">TT</C.Avatar>);
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={chipAvatar} />
    );
  })
  .add('with broken Avatar', () => {
    const chipAvatar = (<C.Avatar size="lg">TT</C.Avatar>);
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={chipAvatar} />
    );
  })
  .add('with Icon', () => {
    const chipIcon = (<C.Icon icon="tag" />);
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={chipIcon} />
    );
  })
  .add('with custom thumbnail', () => {
    const customElement = (
      <div style={{
        border: '2px solid red',
        fontWeight: 'bold',
        textAlign: 'center',
        height: '20px',
        width: '20px',
        borderRadius: '12px',
        fontSize: '12px',
        backgroundColor: 'white'
      }}
      >3
      </div>
    );
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={customElement} />
    );
  })
  .add('with broken custom thumbnail', () => {
    const customElement = (
      <div style={{
        border: '3px solid red',
        fontWeight: 'bold',
        textAlign: 'center',
        height: '40px',
        width: '40px',
        borderRadius: '20px',
        fontSize: '16px',
        backgroundColor: 'white'
      }}
      >3
      </div>
    );
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={customElement} />
    );
  })
  .add('with onDelete', () => {
    function deleteChip() {
      alert('Delete chip');
    }
    return (
      <C.Chip label="Chip Name" color="topaz" onDelete={deleteChip} />
    );
  })
  .add('with onClick and onDelete', () => {
    function clickChip() {
      alert('Clicked chip');
    }
    function deleteChip() {
      alert('Delete chip');
    }
    return (
      <C.Chip label="Chip Name" color="topaz" onClick={clickChip} onDelete={deleteChip} />
    );
  })
  .add('with onClick, onDelete, Avatar', () => {
    function clickChip() {
      alert('Clicked chip');
    }
    function deleteChip() {
      alert('Delete chip');
    }
    const chipAvatar = (<C.Avatar src="https://api.pnl.gov/operations/v2/portraits/self?type=image" size="xs" />);
    return (
      <C.Chip label="Chip Name" color="garnet" thumbnail={chipAvatar} onClick={clickChip} onDelete={deleteChip} />
    );
  })
  .add('small with label', () => (
    <C.Chip label="Chip Name" size="sm" />
  ))
  .add('small with Icon', () => {
    const chipIcon = (<C.Icon icon="tag" />);
    return (
      <C.Chip label="Chip Name" color="topaz" size="sm" thumbnail={chipIcon} />
    );
  })
  .add('small with onClick, onDelete, Avatar', () => {
    function clickChip() {
      alert('Clicked chip');
    }
    function deleteChip() {
      alert('Delete chip');
    }
    const chipAvatar = (<C.Avatar src="https://api.pnl.gov/operations/v2/portraits/self?type=image" size="xs" />);
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={chipAvatar} onClick={clickChip} onDelete={deleteChip} size="sm" />
    );
  })
  .add('extra small with label', () => (
    <C.Chip label="Chip Name" size="xs" />
  ))
  .add('extra small with Icon', () => {
    const chipIcon = (<C.Icon icon="tag" />);
    return (
      <C.Chip label="Chip Name" color="topaz" size="xs" thumbnail={chipIcon} />
    );
  })
  .add('large with label', () => (
    <C.Chip label="Chip Name" size="lg" />
  ))
  .add('large with Icon', () => {
    const chipIcon = (<C.Icon icon="tag" />);
    return (
      <C.Chip label="Chip Name" color="topaz" size="lg" thumbnail={chipIcon} />
    );
  })
  .add('large with text Avatar', () => {
    const chipAvatar = (<C.Avatar size="sm">TT</C.Avatar>);
    return (
      <C.Chip label="Chip Name" color="topaz" size="lg" thumbnail={chipAvatar} />
    );
  })
  .add('large with onClick, onDelete, Avatar', () => {
    function clickChip() {
      alert('Clicked chip');
    }
    function deleteChip() {
      alert('Delete chip');
    }
    const chipAvatar = (<C.Avatar src="https://api.pnl.gov/operations/v2/portraits/self?type=image" size="xs" />);
    return (
      <C.Chip label="Chip Name" color="topaz" thumbnail={chipAvatar} onClick={clickChip} onDelete={deleteChip} size="lg" />
    );
  });
