import { maskIds, masks } from './icon-masks'; // eslint-disable-line

/*
* During test writing you can selectively run just these tests or skip individual ones while you
* work on others https://mochajs.org/#exclusive-tests
*/
describe('Icon-Mask Library: ', () => {
  describe('checking for import errors', () => {
    it('should be in the components helper directory', () => {
      expect(maskIds).toBeTruthy();
      expect(masks).toBeTruthy();
    });
  });
});
