/* eslint import/no-extraneous-dependencies: ["error", {"devDependencies": true}] */
import 'whatwg-fetch';
import React from 'react';
import { storiesOf } from '@storybook/react'; // eslint-disable-line
import { boolean } from '@storybook/addon-knobs/react'; // eslint-disable-line

import * as C from '../index';

storiesOf('LoaderWrapper', module)
  .add('with default options', () => (
    <C.LoaderWrapper show={boolean('Is Loading', true)}>
      <p>I&rsquo;m hidden until the wrapper&rsquo;s show property is falsey.</p>
    </C.LoaderWrapper>
  ));
