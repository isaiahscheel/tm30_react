/* eslint import/no-extraneous-dependencies: ["error", {"devDependencies": true}] */
import 'whatwg-fetch';
import React from 'react';
import { storiesOf } from '@storybook/react'; // eslint-disable-line
import { action } from '@storybook/addon-actions'; // eslint-disable-line

import * as C from '../index';

storiesOf('ProgressDetail', module)
  .add('with step details in card', () => (
    <C.Card>
      <C.Progress activeStep={1}>
        <C.ProgressStep key="1" icon="plane" label="Testing 1" />
        <C.ProgressStep key="2" icon="plane" onClick={action('clicked step 2')} label="Testing 2" />
        <C.ProgressStep key="3" icon="plane" onClick={action('clicked step 3')} label="Testing 3" />
        <C.ProgressStep key="4" icon="plane" onClick={action('clicked step 4')} label="Testing 4" />
        <C.ProgressStep key="5" icon="plane" onClick={action('clicked step 5')} label="Testing 5" />
        <C.ProgressStep key="6" icon="plane" onClick={action('clicked step 6')} label="Testing 6" />
      </C.Progress>
      <C.ProgressDetail progressPct={20}>
        <h3>Step Number 2</h3>
        <p>Step Details</p>
      </C.ProgressDetail>
    </C.Card>
  ));
