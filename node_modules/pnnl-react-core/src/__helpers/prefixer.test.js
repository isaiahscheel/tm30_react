import prefixer from './prefixer'; // eslint-disable-line

/*
* During test writing you can selectively run just these tests or skip individual ones while you
* work on others https://facebook.github.io/jest/docs/en/api.html#testskipname-fn
*/
describe('Prefixer Library: ', () => {
  describe('checking for import errors', () => {
    it('should be in the components helper directory', () => {
      expect(prefixer).toBeTruthy();
    });
  });
});
